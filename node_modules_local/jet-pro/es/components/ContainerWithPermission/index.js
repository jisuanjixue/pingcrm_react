import React from 'react';
import { hasPermission } from '../../utils/stringUtils';

/**
 * 权限props
 */

/**
 * 带权限检查的容器
 */
var ContainerWithPermission = function ContainerWithPermission(_ref) {
  var required = _ref.required,
    has = _ref.has,
    children = _ref.children;
  if (!hasPermission(required, has)) return /*#__PURE__*/ React.createElement(React.Fragment, null);
  return /*#__PURE__*/ React.createElement(React.Fragment, null, children);
};
export default ContainerWithPermission;
