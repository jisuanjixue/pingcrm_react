import { Store } from 'antd/es/form/interface';
import React from 'react';
import type { IModalFormProps } from './interface';
/**
 *  对话框表单
 */
declare const ModalForm: <TDetailView extends Store, TSaveView = TDetailView>({
  modalProps,
  visible,
  title,
  width,
  footerProps,
  footer,
  footerExtra,
  initDetail,
  onClose,
  onSubmit,
  children,
  ...props
}: IModalFormProps<TDetailView, TSaveView>) => React.JSX.Element;
export default ModalForm;
