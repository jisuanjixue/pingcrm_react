import React from 'react';
import type { IFilterFormItemProps } from './interface';
/**
 * 【jet-pro】number类型的范围FilterItem
 * @param param0
 * @returns
 */
declare const NumberRange: ({
  name,
  label,
  defaultValue,
  and,
}: Omit<
  IFilterFormItemProps<[number | null, number | null]>,
  'type' | 'showFilterType'
>) => React.JSX.Element;
export default NumberRange;
