import type { IFilter } from '../../../../dataTypes/dynamicQuery';
import type { TFilterFormData } from './interface';
/**
 * form data转换为filters
 * @param values form data
 * @returns filters
 */
export declare const convertToFilters: (values: TFilterFormData) => IFilter[];
/**
 * filters转换为form data
 * @param formData form data
 * @param filters filters
 * @returns form data
 */
export declare const convertToFilterFormData: (
  formData: TFilterFormData,
  filters?: IFilter[],
) => TFilterFormData;
