import { createBrowserHistory } from 'history';
import { useRef } from 'react';
export default (function (history) {
  if (history) return history;
  return useRef(createBrowserHistory()).current;
});
