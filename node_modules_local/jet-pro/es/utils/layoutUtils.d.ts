import type { ColProps } from 'antd';
/**
 * 根据缩放计算响应式配置
 * @param scale 缩放（默认为1）
 * @returns
 */
export declare const getColProps: (scale?: number) => ColProps;
export declare const getFormItemProps: (labelScale?: number) => {
  labelCol: {
    span: number;
  };
  wrapperCol: {
    span: number;
  };
};
