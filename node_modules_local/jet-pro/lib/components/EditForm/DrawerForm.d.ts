import { Store } from 'antd/es/form/interface';
import React from 'react';
import type { IDrawerFormProps } from './interface';
/**
 *  抽屉表单
 * @param param
 * @returns
 */
declare const DrawerForm: <TDetailView extends Store, TSaveView = TDetailView>({
  drawerProps,
  visible,
  title,
  width,
  footerProps,
  footer,
  footerExtra,
  initDetail,
  onClose,
  onSubmit,
  children,
  ...props
}: IDrawerFormProps<TDetailView, TSaveView>) => React.JSX.Element;
export default DrawerForm;
