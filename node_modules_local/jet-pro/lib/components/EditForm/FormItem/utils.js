var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all) __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if ((from && typeof from === 'object') || typeof from === 'function') {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, {
          get: () => from[key],
          enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable,
        });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, '__esModule', { value: true }), mod);

// src/components/EditForm/FormItem/utils.ts
var utils_exports = {};
__export(utils_exports, {
  genFormItemProps: () => genFormItemProps,
  getFormItemRequired: () => getFormItemRequired,
  getLabelText: () => getLabelText,
});
module.exports = __toCommonJS(utils_exports);
var import_validators = require('../../../utils/validators');
var getFormItemRequired = (required, tip) =>
  typeof required === 'boolean' && required ? tip : required;
var getLabelText = (label) => {
  var _a;
  if (typeof label === 'string') return label;
  const children = (_a = label == null ? void 0 : label.props) == null ? void 0 : _a.children;
  if (typeof children === 'string') return children;
  return void 0;
};
var genFormItemProps = ({ required, rules, ...props }) => ({
  ...props,
  rules: (0, import_validators.genFormItemRules)(
    required,
    typeof required === 'string' ? required : '不能为空',
    rules,
  ),
});
// Annotate the CommonJS export names for ESM import in node:
0 &&
  (module.exports = {
    genFormItemProps,
    getFormItemRequired,
    getLabelText,
  });
