import React from 'react';
import type { IFilterFormItemProps, ValueDateTimeType } from './interface';
export interface IProps extends IFilterFormItemProps<string> {
  mode?: ValueDateTimeType;
}
declare const _default: ({
  name,
  label,
  type,
  defaultValue,
  and,
  showFilterType,
  mode,
}: IProps) => React.JSX.Element;
export default _default;
