import React from 'react';
import type { IFilterFormItemProps } from './interface';
/**
 * number类型的FilterItem
 * @param param0
 * @returns
 */
declare const Number: ({
  name,
  label,
  type,
  defaultValue,
  showFilterType,
  and,
}: IFilterFormItemProps<number>) => React.JSX.Element;
export default Number;
