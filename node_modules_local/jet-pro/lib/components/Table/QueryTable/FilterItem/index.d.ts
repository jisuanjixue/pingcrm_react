/// <reference types="react" />
declare const _default: {
  DatePicker: ({
    name,
    label,
    type,
    defaultValue,
    and,
    showFilterType,
    mode,
  }: import('./FilterDatePicker').IProps) => import('react').JSX.Element;
  DateRangePicker: ({
    name,
    label,
    defaultValue,
    mode,
    and,
  }: import('./FilterDateRangePicker').IProps) => import('react').JSX.Element;
  Text: ({
    name,
    label,
    type,
    and,
    defaultValue,
    showFilterType,
  }: import('./interface').IFilterFormItemProps<string>) => import('react').JSX.Element;
  Hidden: ({
    name,
    type,
    and,
    defaultValue,
  }: import('./FilterHidden').IProps) => import('react').JSX.Element;
  Number: ({
    name,
    label,
    type,
    defaultValue,
    showFilterType,
    and,
  }: import('./interface').IFilterFormItemProps<number>) => import('react').JSX.Element;
  NumberRange: ({
    name,
    label,
    defaultValue,
    and,
  }: Omit<
    import('./interface').IFilterFormItemProps<[number | null, number | null]>,
    'type' | 'showFilterType'
  >) => import('react').JSX.Element;
  Select: ({
    name,
    label,
    type: propsType,
    and,
    defaultValue,
    showFilterType,
    mode,
    options: propsOptions,
    fieldNames: propsFieldNames,
    componentProps,
  }: import('./FilterSelect').IFilterItemSelectProps) => import('react').JSX.Element;
};
export default _default;
