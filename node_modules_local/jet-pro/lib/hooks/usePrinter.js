var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all) __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if ((from && typeof from === 'object') || typeof from === 'function') {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, {
          get: () => from[key],
          enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable,
        });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (
  (target = mod != null ? __create(__getProtoOf(mod)) : {}),
  __copyProps(
    // If the importer is in node compatibility mode or this is not an ESM
    // file that has been converted to a CommonJS file using a Babel-
    // compatible transform (i.e. "__esModule" has not been set), then set
    // "default" to the CommonJS "module.exports" for node compatibility.
    isNodeMode || !mod || !mod.__esModule
      ? __defProp(target, 'default', { value: mod, enumerable: true })
      : target,
    mod,
  )
);
var __toCommonJS = (mod) => __copyProps(__defProp({}, '__esModule', { value: true }), mod);

// src/hooks/usePrinter.tsx
var usePrinter_exports = {};
__export(usePrinter_exports, {
  default: () => usePrinter_default,
});
module.exports = __toCommonJS(usePrinter_exports);
var import_ahooks = require('ahooks');
var import_react = __toESM(require('react'));
var usePrinter_default = () => {
  const factory = (0, import_ahooks.useCreation)(
    () => ({ key: `${/* @__PURE__ */ new Date().getTime()}` }),
    [],
  );
  const refIFrame = (0, import_react.useRef)();
  const onPrint = (content) => {
    if (!refIFrame.current) return;
    const iframeWindow = refIFrame.current.contentWindow;
    if (!iframeWindow) {
      return;
    }
    const iframeDocument = iframeWindow.document;
    iframeDocument.body.innerHTML = content || '';
    iframeWindow.print();
  };
  return [
    onPrint,
    /* @__PURE__ */ import_react.default.createElement('iframe', {
      key: factory.key,
      style: { display: 'none' },
      ref: (elm) => {
        refIFrame.current = elm;
      },
    }),
  ];
};
