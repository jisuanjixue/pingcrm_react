var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all) __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if ((from && typeof from === 'object') || typeof from === 'function') {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, {
          get: () => from[key],
          enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable,
        });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, '__esModule', { value: true }), mod);

// src/utils/layoutUtils.ts
var layoutUtils_exports = {};
__export(layoutUtils_exports, {
  getColProps: () => getColProps,
  getFormItemProps: () => getFormItemProps,
});
module.exports = __toCommonJS(layoutUtils_exports);
var getColProps = (scale) => {
  const defaultColProps = {
    xs: 24,
    // <576px
    sm: 12,
    // ≥576px
    md: 8,
    // ≥768px
    lg: 8,
    // ≥992px
    xl: 6,
    // ≥1200px
    xxl: 6,
    // ≥1600px
  };
  if (!scale || scale === 1) return defaultColProps;
  const colProps = {};
  Object.keys(defaultColProps).forEach((key) => {
    colProps[key] = Math.round(Number(defaultColProps[key]) * scale);
  });
  return colProps;
};
var getFormItemProps = (labelScale) => {
  const col = 24;
  const labelCol = Math.round((labelScale || 0.3) * col);
  const wrapperCol = col - labelCol;
  return { labelCol: { span: labelCol }, wrapperCol: { span: wrapperCol } };
};
// Annotate the CommonJS export names for ESM import in node:
0 &&
  (module.exports = {
    getColProps,
    getFormItemProps,
  });
