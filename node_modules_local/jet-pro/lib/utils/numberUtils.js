var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all) __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if ((from && typeof from === 'object') || typeof from === 'function') {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, {
          get: () => from[key],
          enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable,
        });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, '__esModule', { value: true }), mod);

// src/utils/numberUtils.ts
var numberUtils_exports = {};
__export(numberUtils_exports, {
  formatCurrency: () => formatCurrency,
  formatNumber: () => formatNumber,
  isNumber: () => isNumber,
  tryParseInt: () => tryParseInt,
});
module.exports = __toCommonJS(numberUtils_exports);
var isNumber = (value) => {
  if (typeof value === 'number') return !Number.isNaN(value);
  return false;
};
var formatCurrency = (value, minimumFractionDigits = 2, showStyle = true) => {
  if (!isNumber(value)) return '';
  const moneyIntl = new Intl.NumberFormat('zh-Hans-CN', {
    currency: 'CNY',
    ...(showStyle ? { style: 'currency' } : {}),
    minimumFractionDigits,
  });
  return moneyIntl.format(value);
};
var formatNumber = (value) => {
  if (!isNumber(value)) return '';
  return new Intl.NumberFormat().format(value);
};
var tryParseInt = (value, defaultValue = 0) => {
  const val = parseInt(value, 10);
  if (isNumber(Number(value))) return val;
  return defaultValue;
};
// Annotate the CommonJS export names for ESM import in node:
0 &&
  (module.exports = {
    formatCurrency,
    formatNumber,
    isNumber,
    tryParseInt,
  });
