/**
 * 字符串有效判断
 * @param value
 * @returns
 */
declare const isNullOrEmpty: (value?: string) => boolean;
/**
 * number左补位
 * @param num
 * @param len 总长度
 * @returns
 */
declare const padLeft: (num?: number, len?: number) => string | undefined;
/**
 *
 * @param required
 * @param has
 * @returns
 */
declare const hasPermission: (required?: string | string[], has?: string | string[]) => boolean;
export { isNullOrEmpty, padLeft, hasPermission };
