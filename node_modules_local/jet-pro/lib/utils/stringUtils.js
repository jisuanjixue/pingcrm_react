var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all) __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if ((from && typeof from === 'object') || typeof from === 'function') {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, {
          get: () => from[key],
          enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable,
        });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, '__esModule', { value: true }), mod);

// src/utils/stringUtils.ts
var stringUtils_exports = {};
__export(stringUtils_exports, {
  hasPermission: () => hasPermission,
  isNullOrEmpty: () => isNullOrEmpty,
  padLeft: () => padLeft,
});
module.exports = __toCommonJS(stringUtils_exports);
var import_numberUtils = require('./numberUtils');
var isNullOrEmpty = (value) => [void 0, null, ''].some((val) => val === value);
var padLeft = (num, len) => {
  if (!(0, import_numberUtils.isNumber)(num) || !(0, import_numberUtils.isNumber)(len))
    return void 0;
  return (Array(len).join('0') + num).slice(-len);
};
var hasPermission = (required, has) => {
  let requiredPs = [];
  let hasPs = [];
  if (required) {
    if (typeof required === 'string') requiredPs = [required];
    else requiredPs = required;
  }
  if (has) {
    if (typeof has === 'string') hasPs = [has];
    else hasPs = has;
  }
  return requiredPs.length
    ? hasPs == null
      ? void 0
      : hasPs.some((hp) => requiredPs.some((rp) => rp === hp))
    : true;
};
// Annotate the CommonJS export names for ESM import in node:
0 &&
  (module.exports = {
    hasPermission,
    isNullOrEmpty,
    padLeft,
  });
